- link ||= nil
- size ||= 's'
- color ||= 'black'
- html_class ||= nil

- hollow ||= false
- textonly ||= false
- confirm ||= true
- top ||= false

- method ||= nil

- tag = link ? 'a' : 'button'
- final_class = cn('shared-button', size, html_class, color, hollow ? 'hollow' : nil, textonly ? 'textonly' : nil)

- if !method.nil?
	= button_to link, method: :delete, class: final_class, form: { data: { turbo_confirm: confirm ? 'Вы уверены?' : nil, turbo_frame: '_top' }, id: "#{method}-form" } do
		%p.stroke{class: "text-#{size}"}= text
- else
	= content_tag tag, { href: link, type: defined?(form) ? 'submit' : nil, data: { turbo_action: top ? 'advance' : nil }, class: final_class } do
		%p.stroke{class: "text-#{size}"}= text
